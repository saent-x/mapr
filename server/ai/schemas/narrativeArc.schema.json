{
  "type": "object",
  "additionalProperties": false,
  "required": ["name", "summary"],
  "properties": {
    "name": {
      "type": "string",
      "minLength": 6,
      "maxLength": 120,
      "description": "Short, sentence-cased descriptive name. Include a region/topic + an approximate time span when natural (e.g. 'Suweida violence Jul-Aug 2026')."
    },
    "summary": {
      "type": "string",
      "minLength": 40,
      "maxLength": 600,
      "description": "Two to four sentence neutral summary of the arc. No new facts beyond the provided event titles + entities."
    },
    "status": {
      "type": "string",
      "enum": ["active", "dormant", "resolved"]
    },
    "tags": {
      "type": "array",
      "maxItems": 6,
      "items": { "type": "string", "minLength": 2, "maxLength": 40 }
    }
  }
}
